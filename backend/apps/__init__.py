"""
apps — Django application packages

Sub-packages:
    apps.users          → Authentication (CustomUser, RefreshToken, JWT)
    apps.links           → URL shortening (ShortURL, validation, redirect)
    apps.analytics      → Click tracking (Click, geo, device breakdowns)
    apps.admin_panel    → Admin operations (platform stats, user/URL management)
    apps.ai_integration → AI features (Gemini slug suggestions, metadata)
"""
